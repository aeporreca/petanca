# Git hooks for Petanca

Run `git config --local core.hooksPath .githooks/` in order to enable the pre-commit 
hook compiling `petanca.sage` to `petanca.py`, so the two files always stay synchronised.
